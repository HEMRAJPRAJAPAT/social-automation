-- This migration's original file was missing from the repository (never
-- committed) even though Postgres recorded it as applied. Recreated as a
-- no-op placeholder so Prisma's migration history is consistent again; the
-- live database schema already reflects whatever this migration did.
SELECT 1;
